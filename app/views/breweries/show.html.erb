
<%= render @brewery %>

<% if @brewery.ratings.empty? %>
 Brewery has not yet been rated!
<% else %>
 Brewery has <%= pluralize(@brewery.ratings.count, 'rating') %> with an average of <%= round(@brewery.average_rating, prec: 3) %>
<% end %>

<div>
  <%= link_to("Back to breweries", breweries_path, class:'btn btn-primary') %>

  <%= edit_and_destroy_buttons(@brewery) %>

  <%= link_to "change activity", toggle_activity_brewery_path(@brewery.id), data: {turbo_method: "post"}, class: "btn btn-primary" %>
</div>
