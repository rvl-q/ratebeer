<table class="table">
  <head>
    <th><%= objects.first.class.name %></th>
    <th>Average</th>
  </head>

  <% objects&.each do |b| %>
    <tr>
      <td>
      <%= b.name %>
      </td>
      <td>
      <%= round(b.average_rating) %>
      </td>
    </tr>
  <% end %>

</table>